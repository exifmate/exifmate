describe('updateMetadata', () => {
  it.todo('calls a Tauri command to write metadata');

  describe('when an image fails to save', () => {
    it.todo('indicates the error');
  });

  describe('when there is a warning', () => {
    it.todo('warns of the warning');
  });
});
